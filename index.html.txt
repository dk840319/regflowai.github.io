<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Regflow AI — Regulatory Intelligence</title>
<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
<link href="https://fonts.googleapis.com/css2?family=Geist:wght@300;400;500;600&family=Geist+Mono:wght@300;400;500&display=swap" rel="stylesheet"/>
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0;}

:root {
  --bg:        #0a0a0a;
  --bg-1:      #111111;
  --bg-2:      #1a1a1a;
  --bg-3:      #222222;
  --border:    rgba(255,255,255,0.08);
  --border-md: rgba(255,255,255,0.12);
  --border-hi: rgba(255,255,255,0.2);
  --text-1:    #ededed;
  --text-2:    #a1a1a1;
  --text-3:    #666666;
  --accent:    #ffffff;
  --blue:      #3b82f6;
  --blue-dim:  rgba(59,130,246,0.15);
  --blue-glow: rgba(59,130,246,0.08);
  --green:     #22c55e;
  --green-dim: rgba(34,197,94,0.12);
  --r:         6px;
  --r-lg:      10px;
  --font:      'Geist', -apple-system, sans-serif;
  --mono:      'Geist Mono', monospace;
  --ease:      cubic-bezier(0.16,1,0.3,1);
}

html { scroll-behavior: smooth; }
body {
  background: var(--bg);
  color: var(--text-1);
  font-family: var(--font);
  font-weight: 400;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  overflow-x: hidden;
}

/* ── Noise overlay ── */
body::before {
  content: '';
  position: fixed; inset: 0;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='1'/%3E%3C/svg%3E");
  opacity: 0.025;
  pointer-events: none;
  z-index: 0;
}

/* ── Nav ── */
nav {
  position: fixed; top: 0; left: 0; right: 0; z-index: 200;
  height: 60px;
  display: flex; align-items: center; justify-content: space-between;
  padding: 0 2rem;
  border-bottom: 1px solid var(--border);
  background: rgba(10,10,10,0.85);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
}
.nav-left { display: flex; align-items: center; gap: 0.6rem; }
.nav-logo-mark {
  width: 28px; height: 28px;
  background: var(--text-1);
  border-radius: 6px;
  display: flex; align-items: center; justify-content: center;
}
.nav-logo-mark svg { width: 16px; height: 16px; }
.nav-wordmark {
  font-size: 0.9rem;
  font-weight: 600;
  color: var(--text-1);
  letter-spacing: -0.02em;
}
.nav-center { display: flex; gap: 0; }
.nav-center a {
  font-size: 0.8rem;
  color: var(--text-2);
  text-decoration: none;
  padding: 0.35rem 0.85rem;
  border-radius: var(--r);
  transition: color 0.2s, background 0.2s;
}
.nav-center a:hover { color: var(--text-1); background: var(--bg-2); }
.nav-right { display: flex; align-items: center; gap: 0.75rem; }
.btn-ghost {
  font-size: 0.8rem; font-weight: 400;
  color: var(--text-2);
  background: none; border: none;
  padding: 0.4rem 0.85rem;
  border-radius: var(--r);
  cursor: pointer;
  transition: color 0.2s, background 0.2s;
  text-decoration: none;
  display: inline-flex;
}
.btn-ghost:hover { color: var(--text-1); background: var(--bg-2); }
.btn-primary {
  font-size: 0.8rem; font-weight: 500;
  color: #000;
  background: var(--text-1);
  border: none;
  padding: 0.45rem 1rem;
  border-radius: var(--r);
  cursor: pointer;
  transition: background 0.2s, transform 0.15s;
  text-decoration: none;
  display: inline-flex; align-items: center; gap: 0.4rem;
}
.btn-primary:hover { background: #d4d4d4; transform: translateY(-1px); }

/* ── Hero ── */
#hero {
  position: relative;
  min-height: 100vh;
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  text-align: center;
  padding: 6rem 2rem 4rem;
  overflow: hidden;
}
.hero-grid-bg {
  position: absolute; inset: 0; z-index: 0;
  background-image:
    linear-gradient(rgba(255,255,255,0.025) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,0.025) 1px, transparent 1px);
  background-size: 40px 40px;
  mask-image: radial-gradient(ellipse 80% 60% at 50% 40%, black 30%, transparent 100%);
}
.hero-glow {
  position: absolute; top: 0; left: 50%; transform: translateX(-50%);
  width: 600px; height: 300px;
  background: radial-gradient(ellipse at center, rgba(59,130,246,0.12) 0%, transparent 70%);
  pointer-events: none; z-index: 0;
}
.hero-inner { position: relative; z-index: 1; max-width: 720px; }
.hero-badge {
  display: inline-flex; align-items: center; gap: 0.5rem;
  background: var(--bg-1);
  border: 1px solid var(--border-md);
  border-radius: 999px;
  padding: 0.3rem 0.9rem 0.3rem 0.5rem;
  font-size: 0.72rem; color: var(--text-2);
  margin-bottom: 2rem;
  font-family: var(--mono);
  letter-spacing: 0.01em;
}
.badge-dot {
  width: 6px; height: 6px; border-radius: 50%;
  background: var(--green);
  box-shadow: 0 0 6px var(--green);
  animation: blink 2.4s ease-in-out infinite;
}
@keyframes blink { 0%,100%{opacity:1;} 50%{opacity:0.35;} }
h1.hero-title {
  font-size: clamp(2.6rem, 6vw, 4.5rem);
  font-weight: 600;
  letter-spacing: -0.04em;
  line-height: 1.05;
  color: var(--text-1);
  margin-bottom: 1.5rem;
}
h1.hero-title .dim { color: var(--text-3); font-weight: 300; }
.hero-sub {
  font-size: 1rem;
  color: var(--text-2);
  max-width: 520px;
  margin: 0 auto 2.5rem;
  line-height: 1.75;
  font-weight: 400;
}
.hero-actions { display: flex; align-items: center; justify-content: center; gap: 0.75rem; flex-wrap: wrap; }
.btn-primary-lg {
  font-size: 0.875rem; font-weight: 500;
  color: #000; background: var(--text-1);
  border: none; padding: 0.65rem 1.4rem;
  border-radius: var(--r);
  cursor: pointer; text-decoration: none;
  display: inline-flex; align-items: center; gap: 0.5rem;
  transition: background 0.2s, transform 0.15s;
}
.btn-primary-lg:hover { background: #d4d4d4; transform: translateY(-1px); }
.btn-outline-lg {
  font-size: 0.875rem; font-weight: 400;
  color: var(--text-2); background: transparent;
  border: 1px solid var(--border-md);
  padding: 0.65rem 1.4rem; border-radius: var(--r);
  cursor: pointer; text-decoration: none;
  display: inline-flex; align-items: center; gap: 0.5rem;
  transition: border-color 0.2s, color 0.2s, transform 0.15s;
}
.btn-outline-lg:hover { border-color: var(--border-hi); color: var(--text-1); transform: translateY(-1px); }

/* Hero stats strip */
.hero-stats {
  display: flex; align-items: center; justify-content: center;
  gap: 0; margin-top: 5rem;
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  background: var(--bg-1);
  overflow: hidden;
  max-width: 680px; margin-left: auto; margin-right: auto;
  margin-top: 5rem;
}
.hstat {
  flex: 1; padding: 1.25rem 1.5rem;
  border-right: 1px solid var(--border);
  text-align: center;
}
.hstat:last-child { border-right: none; }
.hstat-val {
  font-size: 1.5rem; font-weight: 600;
  color: var(--text-1); letter-spacing: -0.03em;
  line-height: 1;
}
.hstat-lbl { font-size: 0.72rem; color: var(--text-3); margin-top: 0.3rem; }

/* ── Sections ── */
.section { padding: 7rem 2rem; max-width: 1100px; margin: 0 auto; }
.section-eyebrow {
  font-family: var(--mono);
  font-size: 0.7rem; letter-spacing: 0.08em; text-transform: uppercase;
  color: var(--text-3); margin-bottom: 0.75rem;
}
.section-title {
  font-size: clamp(1.8rem, 4vw, 2.8rem);
  font-weight: 600; letter-spacing: -0.035em; line-height: 1.1;
  color: var(--text-1); margin-bottom: 1rem;
}
.section-desc {
  font-size: 0.95rem; color: var(--text-2);
  max-width: 500px; line-height: 1.75;
}
.section-head { margin-bottom: 3.5rem; }

/* ── Border separator ── */
.sep { border: none; border-top: 1px solid var(--border); margin: 0; }

/* ── Product cards ── */
.product-grid {
  display: grid;
  grid-template-columns: 1.5fr 1fr;
  gap: 1px;
  background: var(--border);
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  overflow: hidden;
  margin-bottom: 1px;
}
.product-grid-row2 {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 1px;
  background: var(--border);
  border: 1px solid var(--border);
  border-top: none;
  border-radius: 0 0 var(--r-lg) var(--r-lg);
  overflow: hidden;
}
.pcard {
  background: var(--bg-1);
  padding: 2rem;
  transition: background 0.2s;
}
.pcard:hover { background: var(--bg-2); }
.pcard-eyebrow {
  display: inline-flex; align-items: center; gap: 0.4rem;
  font-family: var(--mono); font-size: 0.65rem;
  letter-spacing: 0.08em; text-transform: uppercase;
  color: var(--blue); margin-bottom: 1rem;
  background: var(--blue-dim);
  padding: 0.25rem 0.6rem; border-radius: 4px;
  border: 1px solid rgba(59,130,246,0.25);
}
.pcard-name {
  font-size: 1.2rem; font-weight: 600;
  letter-spacing: -0.02em; color: var(--text-1);
  margin-bottom: 0.6rem;
}
.pcard-desc {
  font-size: 0.85rem; color: var(--text-2);
  line-height: 1.75; margin-bottom: 1.5rem;
}
.pcard-features { list-style: none; display: flex; flex-direction: column; gap: 0.5rem; margin-bottom: 1.75rem; }
.pcard-features li {
  font-size: 0.8rem; color: var(--text-2);
  display: flex; align-items: flex-start; gap: 0.6rem;
}
.pcard-features li::before {
  content: '';
  width: 14px; height: 14px; flex-shrink: 0; margin-top: 1px;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 14 14' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M2.5 7l3 3 6-6' stroke='%2322c55e' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'/%3E%3C/svg%3E");
}
.pcard-link {
  font-size: 0.78rem; font-weight: 500;
  color: var(--text-2); text-decoration: none;
  display: inline-flex; align-items: center; gap: 0.35rem;
  border: 1px solid var(--border-md);
  padding: 0.4rem 0.85rem; border-radius: var(--r);
  transition: border-color 0.2s, color 0.2s;
}
.pcard-link:hover { border-color: var(--border-hi); color: var(--text-1); }
.pcard-metrics {
  display: flex; flex-direction: column; justify-content: center;
  gap: 1.25rem;
}
.pm-row { display: flex; flex-direction: column; }
.pm-val {
  font-size: 1.75rem; font-weight: 600;
  letter-spacing: -0.04em; color: var(--text-1); line-height: 1;
}
.pm-lbl { font-size: 0.72rem; color: var(--text-3); margin-top: 0.25rem; }
.pm-divider { height: 1px; background: var(--border); }
.mini-pcard { background: var(--bg-1); padding: 1.75rem; transition: background 0.2s; }
.mini-pcard:hover { background: var(--bg-2); }
.mini-icon {
  width: 32px; height: 32px; border-radius: var(--r);
  background: var(--bg-3); border: 1px solid var(--border-md);
  display: flex; align-items: center; justify-content: center;
  font-size: 0.95rem; margin-bottom: 1rem;
}
.mini-name {
  font-size: 0.9rem; font-weight: 600;
  color: var(--text-1); letter-spacing: -0.015em; margin-bottom: 0.4rem;
}
.mini-desc { font-size: 0.78rem; color: var(--text-2); line-height: 1.7; }

/* ── Achievements ── */
#achievements { background: var(--bg-1); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }
#achievements .inner { max-width: 1100px; margin: 0 auto; padding: 7rem 2rem; }
.ach-grid {
  display: grid;
  grid-template-columns: repeat(4,1fr);
  gap: 1px; background: var(--border);
  border: 1px solid var(--border);
  border-radius: var(--r-lg); overflow: hidden;
  margin-bottom: 4rem;
}
.ach-cell {
  background: var(--bg-1); padding: 2rem;
  transition: background 0.2s;
}
.ach-cell:hover { background: var(--bg-2); }
.ach-num {
  font-size: 2.25rem; font-weight: 600;
  letter-spacing: -0.045em; color: var(--text-1);
  line-height: 1; margin-bottom: 0.5rem;
}
.ach-label { font-size: 0.82rem; color: var(--text-2); margin-bottom: 0.25rem; font-weight: 500; }
.ach-sub { font-size: 0.72rem; color: var(--text-3); line-height: 1.5; }

/* Timeline */
.tl { border: 1px solid var(--border); border-radius: var(--r-lg); overflow: hidden; }
.tl-item {
  display: grid; grid-template-columns: 180px 1fr;
  border-bottom: 1px solid var(--border);
  transition: background 0.2s;
}
.tl-item:last-child { border-bottom: none; }
.tl-item:hover { background: var(--bg-2); }
.tl-left {
  padding: 1.75rem 2rem;
  border-right: 1px solid var(--border);
  display: flex; flex-direction: column; justify-content: flex-start;
}
.tl-date { font-family: var(--mono); font-size: 0.68rem; color: var(--text-3); letter-spacing: 0.05em; }
.tl-co {
  font-size: 0.78rem; font-weight: 500; color: var(--text-2);
  margin-top: 0.35rem;
}
.tl-right { padding: 1.75rem 2rem; }
.tl-role {
  font-size: 0.95rem; font-weight: 600;
  color: var(--text-1); letter-spacing: -0.015em; margin-bottom: 0.5rem;
}
.tl-desc { font-size: 0.82rem; color: var(--text-2); line-height: 1.75; }

/* ── No-code section ── */
.nocode-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1px; background: var(--border);
  border: 1px solid var(--border);
  border-radius: var(--r-lg); overflow: hidden;
}
.nc-cell { background: var(--bg-1); padding: 2rem; transition: background 0.2s; }
.nc-cell:hover { background: var(--bg-2); }
.nc-icon {
  width: 36px; height: 36px; border-radius: var(--r);
  background: var(--bg-3); border: 1px solid var(--border-md);
  display: flex; align-items: center; justify-content: center;
  font-size: 1rem; margin-bottom: 1.25rem;
}
.nc-title { font-size: 0.9rem; font-weight: 600; color: var(--text-1); margin-bottom: 0.5rem; }
.nc-desc { font-size: 0.8rem; color: var(--text-2); line-height: 1.7; }

/* ── Testimonial ── */
.testimonial-grid {
  display: grid; grid-template-columns: repeat(3,1fr);
  gap: 1px; background: var(--border);
  border: 1px solid var(--border);
  border-radius: var(--r-lg); overflow: hidden;
}
.tcard { background: var(--bg-1); padding: 1.75rem; transition: background 0.2s; }
.tcard:hover { background: var(--bg-2); }
.tcard-quote { font-size: 0.85rem; color: var(--text-2); line-height: 1.8; margin-bottom: 1.5rem; font-style: italic; }
.tcard-author { display: flex; align-items: center; gap: 0.65rem; }
.tcard-avatar {
  width: 30px; height: 30px; border-radius: 50%;
  background: var(--bg-3); border: 1px solid var(--border-md);
  display: flex; align-items: center; justify-content: center;
  font-size: 0.72rem; font-weight: 600; color: var(--text-2);
}
.tcard-name { font-size: 0.78rem; font-weight: 500; color: var(--text-1); }
.tcard-role { font-size: 0.7rem; color: var(--text-3); }

/* ── CTA / Contact ── */
#contact {
  text-align: center; padding: 8rem 2rem;
  position: relative; overflow: hidden;
}
.cta-glow {
  position: absolute; bottom: -50px; left: 50%; transform: translateX(-50%);
  width: 500px; height: 300px;
  background: radial-gradient(ellipse, rgba(59,130,246,0.1) 0%, transparent 70%);
  pointer-events: none;
}
.cta-inner { position: relative; z-index: 1; max-width: 560px; margin: 0 auto; }
.cta-tag {
  display: inline-block;
  font-family: var(--mono); font-size: 0.68rem;
  letter-spacing: 0.08em; text-transform: uppercase;
  color: var(--text-3); margin-bottom: 1.5rem;
  border: 1px solid var(--border); border-radius: 999px;
  padding: 0.25rem 0.75rem;
}
.cta-title {
  font-size: clamp(2rem, 5vw, 3.2rem);
  font-weight: 600; letter-spacing: -0.04em; line-height: 1.1;
  color: var(--text-1); margin-bottom: 1rem;
}
.cta-desc { font-size: 0.95rem; color: var(--text-2); line-height: 1.75; margin-bottom: 2.5rem; }
.cta-actions { display: flex; justify-content: center; gap: 0.75rem; flex-wrap: wrap; margin-bottom: 2.5rem; }
.social-row { display: flex; justify-content: center; gap: 0.5rem; flex-wrap: wrap; }
.social-btn {
  font-size: 0.75rem; color: var(--text-3);
  border: 1px solid var(--border); border-radius: var(--r);
  padding: 0.4rem 0.85rem; text-decoration: none;
  display: inline-flex; align-items: center; gap: 0.4rem;
  transition: border-color 0.2s, color 0.2s;
}
.social-btn:hover { border-color: var(--border-hi); color: var(--text-2); }

/* ── SaaS Tools ── */
.tools-section { background: var(--bg); }
.tools-header { margin-bottom: 3.5rem; }
.tools-duo {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1px; background: var(--border);
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  overflow: hidden;
  margin-bottom: 1px;
}
.tool-card {
  background: var(--bg-1);
  padding: 2.5rem;
  display: flex; flex-direction: column;
  transition: background 0.2s;
  position: relative;
  overflow: hidden;
}
.tool-card:hover { background: var(--bg-2); }
.tool-card-accent {
  position: absolute; top: 0; left: 0; right: 0;
  height: 2px;
}
.accent-blue { background: linear-gradient(90deg, var(--blue) 0%, transparent 100%); }
.accent-green { background: linear-gradient(90deg, var(--green) 0%, transparent 100%); }
.tool-top { display: flex; align-items: flex-start; justify-content: space-between; margin-bottom: 1.5rem; }
.tool-icon-wrap {
  width: 44px; height: 44px; border-radius: var(--r-lg);
  background: var(--bg-3); border: 1px solid var(--border-md);
  display: flex; align-items: center; justify-content: center;
  font-size: 1.2rem;
}
.tool-badge-paid {
  font-family: var(--mono); font-size: 0.62rem;
  letter-spacing: 0.06em; text-transform: uppercase;
  background: var(--blue-dim); color: var(--blue);
  border: 1px solid rgba(59,130,246,0.25);
  padding: 0.2rem 0.6rem; border-radius: 4px;
}
.tool-badge-free {
  font-family: var(--mono); font-size: 0.62rem;
  letter-spacing: 0.06em; text-transform: uppercase;
  background: var(--green-dim); color: var(--green);
  border: 1px solid rgba(34,197,94,0.25);
  padding: 0.2rem 0.6rem; border-radius: 4px;
}
.tool-name {
  font-size: 1.3rem; font-weight: 600;
  letter-spacing: -0.025em; color: var(--text-1);
  margin-bottom: 0.35rem; line-height: 1.2;
}
.tool-tagline {
  font-family: var(--mono); font-size: 0.68rem;
  color: var(--text-3); letter-spacing: 0.04em;
  margin-bottom: 1rem;
}
.tool-desc {
  font-size: 0.85rem; color: var(--text-2);
  line-height: 1.8; margin-bottom: 1.75rem; flex: 1;
}
.tool-features { list-style: none; display: flex; flex-direction: column; gap: 0.5rem; margin-bottom: 2rem; }
.tool-features li {
  font-size: 0.8rem; color: var(--text-2);
  display: flex; align-items: flex-start; gap: 0.6rem;
}
.tool-features li .fi {
  width: 14px; height: 14px; flex-shrink: 0; margin-top: 1px;
  display: inline-block;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 14 14' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M2.5 7l3 3 6-6' stroke='%2322c55e' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'/%3E%3C/svg%3E");
}
.tool-features li .fi-blue {
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 14 14' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M2.5 7l3 3 6-6' stroke='%233b82f6' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'/%3E%3C/svg%3E");
}
.tool-stack-row {
  display: flex; gap: 0.4rem; flex-wrap: wrap; margin-bottom: 2rem;
}
.stack-chip {
  font-family: var(--mono); font-size: 0.62rem;
  letter-spacing: 0.04em; text-transform: uppercase;
  color: var(--text-3); background: var(--bg-3);
  border: 1px solid var(--border); border-radius: 4px;
  padding: 0.2rem 0.55rem;
}
.tool-actions { display: flex; gap: 0.6rem; flex-wrap: wrap; }
.tool-btn-primary {
  font-size: 0.78rem; font-weight: 500;
  color: #000; background: var(--text-1);
  border: none; padding: 0.5rem 1.1rem;
  border-radius: var(--r); cursor: pointer;
  text-decoration: none; display: inline-flex;
  align-items: center; gap: 0.4rem;
  transition: background 0.2s, transform 0.15s;
}
.tool-btn-primary:hover { background: #d4d4d4; transform: translateY(-1px); }
.tool-btn-outline {
  font-size: 0.78rem; font-weight: 400;
  color: var(--text-2); background: transparent;
  border: 1px solid var(--border-md);
  padding: 0.5rem 1.1rem; border-radius: var(--r);
  cursor: pointer; text-decoration: none;
  display: inline-flex; align-items: center; gap: 0.4rem;
  transition: border-color 0.2s, color 0.2s;
}
.tool-btn-outline:hover { border-color: var(--border-hi); color: var(--text-1); }
.tool-divider-row {
  display: grid; grid-template-columns: repeat(3,1fr);
  gap: 1px; background: var(--border);
  border: 1px solid var(--border);
  border-top: none;
  border-radius: 0 0 var(--r-lg) var(--r-lg);
  overflow: hidden;
}
.tool-stat-cell {
  background: var(--bg-1); padding: 1.25rem 1.75rem;
  transition: background 0.2s;
  display: flex; flex-direction: column;
}
.tool-stat-cell:hover { background: var(--bg-2); }
.tsc-val {
  font-size: 1.35rem; font-weight: 600;
  letter-spacing: -0.03em; color: var(--text-1); line-height: 1;
}
.tsc-lbl { font-size: 0.7rem; color: var(--text-3); margin-top: 0.3rem; }
@media(max-width:900px){
  .tools-duo { grid-template-columns: 1fr; }
  .tool-divider-row { grid-template-columns: 1fr 1fr; }
}
@media(max-width:640px){
  .tool-divider-row { grid-template-columns: 1fr; }
}

/* ── Footer ── */
footer {
  border-top: 1px solid var(--border);
  padding: 1.5rem 2rem;
  display: flex; align-items: center; justify-content: space-between;
  max-width: 100%;
}
.footer-brand { display: flex; align-items: center; gap: 0.5rem; }
.footer-brand-name { font-size: 0.82rem; font-weight: 600; color: var(--text-2); letter-spacing: -0.01em; }
.footer-copy { font-size: 0.72rem; color: var(--text-3); font-family: var(--mono); }
.footer-links { display: flex; gap: 1.25rem; }
.footer-links a { font-size: 0.72rem; color: var(--text-3); text-decoration: none; transition: color 0.2s; }
.footer-links a:hover { color: var(--text-2); }

/* ── Reveal ── */
.reveal { opacity:0; transform:translateY(24px); transition: opacity 0.7s var(--ease), transform 0.7s var(--ease); }
.reveal.visible { opacity:1; transform:translateY(0); }
.reveal-d1 { transition-delay:0.05s; }
.reveal-d2 { transition-delay:0.12s; }
.reveal-d3 { transition-delay:0.2s; }

/* ── Responsive ── */
@media(max-width:900px){
  .product-grid{ grid-template-columns:1fr; }
  .product-grid-row2 { grid-template-columns:1fr; }
  .ach-grid { grid-template-columns:repeat(2,1fr); }
  .nocode-grid { grid-template-columns:1fr; }
  .testimonial-grid { grid-template-columns:1fr; }
  .tl-item { grid-template-columns:1fr; }
  .tl-left { border-right:none; border-bottom:1px solid var(--border); padding:1.25rem 1.5rem; }
}
@media(max-width:640px){
  nav { padding:0 1.25rem; }
  .nav-center { display:none; }
  .section { padding:5rem 1.25rem; }
  #achievements .inner { padding:5rem 1.25rem; }
  footer { flex-direction:column; gap:1rem; text-align:center; }
  .footer-links { justify-content:center; }
  .hero-stats { flex-direction:column; border-radius:var(--r-lg); }
  .hstat { border-right:none; border-bottom:1px solid var(--border); }
  .hstat:last-child { border-bottom:none; }
}
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="nav-left">
    <div class="nav-logo-mark">
      <svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
        <rect x="2" y="2" width="5" height="5" rx="1" fill="#0a0a0a"/>
        <rect x="9" y="2" width="5" height="5" rx="1" fill="#0a0a0a"/>
        <rect x="2" y="9" width="5" height="5" rx="1" fill="#0a0a0a"/>
        <rect x="9" y="9" width="5" height="5" rx="1" fill="#0a0a0a" opacity="0.4"/>
      </svg>
    </div>
    <span class="nav-wordmark">Regflow AI</span>
  </div>
  <div class="nav-center">
    <a href="#product">Product</a>
    <a href="#tools">Tools</a>
    <a href="#achievements">Journey</a>
    <a href="#approach">Approach</a>
    <a href="#contact">Contact</a>
  </div>
  <div class="nav-right">
    <a href="#" class="btn-ghost">Sign in</a>
    <a href="#contact" class="btn-primary">
      Get early access
      <svg width="12" height="12" viewBox="0 0 12 12" fill="none"><path d="M2.5 6h7M6.5 3.5L9 6l-2.5 2.5" stroke="currentColor" stroke-width="1.25" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </a>
  </div>
</nav>

<!-- HERO -->
<section id="hero">
  <div class="hero-grid-bg"></div>
  <div class="hero-glow"></div>
  <div class="hero-inner">
    <div class="hero-badge reveal">
      <div class="badge-dot"></div>
      Now in early access · Regulatory Intelligence Platform
    </div>
    <h1 class="hero-title reveal reveal-d1">
      Regulatory intelligence<br/>
      <span class="dim">built for founders</span><br/>
      who move fast.
    </h1>
    <p class="hero-sub reveal reveal-d2">Regflow AI monitors global regulatory changes, drafts compliance summaries, and alerts your team — so you can build without watching the rulebook yourself.</p>
    <div class="hero-actions reveal reveal-d3">
      <a href="#contact" class="btn-primary-lg">
        Start for free
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7h8M8 4.5L10.5 7 8 9.5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </a>
      <a href="#product" class="btn-outline-lg">See how it works</a>
    </div>
    <div class="hero-stats reveal" style="transition-delay:0.3s">
      <div class="hstat">
        <div class="hstat-val">140+</div>
        <div class="hstat-lbl">Regulations tracked</div>
      </div>
      <div class="hstat">
        <div class="hstat-val">500+</div>
        <div class="hstat-lbl">Early users</div>
      </div>
      <div class="hstat">
        <div class="hstat-val">18</div>
        <div class="hstat-lbl">Countries covered</div>
      </div>
      <div class="hstat">
        <div class="hstat-val">4.9★</div>
        <div class="hstat-lbl">User rating</div>
      </div>
    </div>
  </div>
</section>

<hr class="sep"/>

<!-- PRODUCT -->
<section id="product" class="section">
  <div class="section-head reveal">
    <p class="section-eyebrow">Product</p>
    <h2 class="section-title">One platform.<br/>Zero compliance blind spots.</h2>
    <p class="section-desc">Regflow AI is designed for non-technical founders who need to stay ahead of regulatory changes without hiring a legal team.</p>
  </div>

  <div class="product-grid reveal">
    <div class="pcard">
      <div class="pcard-eyebrow">
        <svg width="10" height="10" viewBox="0 0 10 10" fill="none"><circle cx="5" cy="5" r="4" stroke="#3b82f6" stroke-width="1.2"/><path d="M5 3v2.5l1.5 1" stroke="#3b82f6" stroke-width="1.2" stroke-linecap="round"/></svg>
        Flagship · 2025
      </div>
      <p class="pcard-name">Regflow AI — Regulatory Intelligence</p>
      <p class="pcard-desc">An AI-powered compliance radar that watches global regulatory updates across 18 countries, extracts what's relevant to your business, and delivers plain-English summaries your team can actually act on.</p>
      <ul class="pcard-features">
        <li>Real-time monitoring of 140+ global regulatory bodies</li>
        <li>Plain-English AI summaries — no legal jargon</li>
        <li>Slack &amp; email alerts for changes that affect your sector</li>
        <li>One-click compliance checklist generation</li>
        <li>Built entirely with no-code and vibe coding tools</li>
      </ul>
      <a href="#contact" class="pcard-link">
        Get early access
        <svg width="12" height="12" viewBox="0 0 12 12" fill="none"><path d="M2.5 6h7M6.5 3.5L9 6l-2.5 2.5" stroke="currentColor" stroke-width="1.25" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </a>
    </div>
    <div class="pcard">
      <div class="pcard-metrics">
        <div class="pm-row">
          <div class="pm-val">500+</div>
          <div class="pm-lbl">Early access users</div>
        </div>
        <div class="pm-divider"></div>
        <div class="pm-row">
          <div class="pm-val">140+</div>
          <div class="pm-lbl">Regulations monitored</div>
        </div>
        <div class="pm-divider"></div>
        <div class="pm-row">
          <div class="pm-val">18</div>
          <div class="pm-lbl">Countries covered</div>
        </div>
        <div class="pm-divider"></div>
        <div class="pm-row">
          <div class="pm-val">#1</div>
          <div class="pm-lbl">Product Hunt launch</div>
        </div>
      </div>
    </div>
  </div>

  <div class="product-grid-row2 reveal reveal-d1">
    <div class="mini-pcard">
      <div class="mini-icon">📋</div>
      <p class="mini-name">Compliance Autopilot</p>
      <p class="mini-desc">Automated weekly digests that summarise every regulation update relevant to your industry. Delivered every Monday morning.</p>
    </div>
    <div class="mini-pcard">
      <div class="mini-icon">🔔</div>
      <p class="mini-name">RegAlert</p>
      <p class="mini-desc">Instant Slack and email notifications the moment a regulation changes in your sector. No more manual monitoring.</p>
    </div>
    <div class="mini-pcard">
      <div class="mini-icon">🗂️</div>
      <p class="mini-name">Checklist Builder</p>
      <p class="mini-desc">Turn any regulation into a step-by-step action checklist your team can assign, track, and close out — in one click.</p>
    </div>
  </div>
</section>

<hr class="sep"/>

<!-- ACHIEVEMENTS -->
<section id="achievements">
  <div class="inner">
    <div class="section-head reveal">
      <p class="section-eyebrow">Journey &amp; Achievements</p>
      <h2 class="section-title">From zero to launch,<br/>no code required.</h2>
      <p class="section-desc">Built Regflow AI from scratch as a non-technical founder — proving that the best products come from deep domain knowledge, not engineering backgrounds.</p>
    </div>

    <div class="ach-grid reveal">
      <div class="ach-cell">
        <div class="ach-num">500+</div>
        <div class="ach-label">Early users</div>
        <div class="ach-sub">Signed up before public launch</div>
      </div>
      <div class="ach-cell">
        <div class="ach-num">140+</div>
        <div class="ach-label">Regulations tracked</div>
        <div class="ach-sub">Across 18 countries globally</div>
      </div>
      <div class="ach-cell">
        <div class="ach-num">#1</div>
        <div class="ach-label">Product Hunt</div>
        <div class="ach-sub">Product of the Day at launch</div>
      </div>
      <div class="ach-cell">
        <div class="ach-num">0</div>
        <div class="ach-label">Lines of code written</div>
        <div class="ach-sub">Fully built with no-code tools</div>
      </div>
    </div>

    <div class="tl reveal">
      <div class="tl-item">
        <div class="tl-left">
          <div class="tl-date">2025 — Now</div>
          <div class="tl-co">Regflow AI · Founder</div>
        </div>
        <div class="tl-right">
          <p class="tl-role">Founded &amp; launched Regflow AI</p>
          <p class="tl-desc">Identified a gap in regulatory intelligence for early-stage startups. Built the entire product using no-code and AI tools — zero engineering background. Onboarded 500+ early users and launched to #1 on Product Hunt.</p>
        </div>
      </div>
      <div class="tl-item">
        <div class="tl-left">
          <div class="tl-date">2023 — 2024</div>
          <div class="tl-co">Domain Research</div>
        </div>
        <div class="tl-right">
          <p class="tl-role">Deep regulatory research &amp; validation</p>
          <p class="tl-desc">Spent 12 months studying regulatory compliance across fintech, healthtech, and SaaS sectors. Interviewed 80+ founders about their compliance pain points. Validated the product thesis before writing a single line of code.</p>
        </div>
      </div>
      <div class="tl-item">
        <div class="tl-left">
          <div class="tl-date">2022 — 2023</div>
          <div class="tl-co">Consulting</div>
        </div>
        <div class="tl-right">
          <p class="tl-role">Regulatory compliance consultant</p>
          <p class="tl-desc">Worked independently advising early-stage startups on regulatory strategy across EU and US markets. This hands-on experience became the foundation for Regflow AI's feature set.</p>
        </div>
      </div>
      <div class="tl-item">
        <div class="tl-left">
          <div class="tl-date">Pre-2022</div>
          <div class="tl-co">Education</div>
        </div>
        <div class="tl-right">
          <p class="tl-role">Law &amp; Policy background</p>
          <p class="tl-desc">Academic background in law and public policy. Specialised in technology regulation and data privacy frameworks — the knowledge that powers Regflow AI's intelligence engine.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<hr class="sep"/>

<!-- APPROACH -->
<section id="approach" class="section">
  <div class="section-head reveal">
    <p class="section-eyebrow">The Approach</p>
    <h2 class="section-title">No-code. No excuses.<br/>Just ship.</h2>
    <p class="section-desc">Regflow AI proves that domain expertise beats engineering credentials every time. Here's the stack that made it possible.</p>
  </div>
  <div class="nocode-grid reveal">
    <div class="nc-cell">
      <div class="nc-icon">🧱</div>
      <p class="nc-title">Built with no-code tools</p>
      <p class="nc-desc">Framer for the website, Bubble for the app, Zapier for automations, and Airtable as the database. No engineering degree needed.</p>
    </div>
    <div class="nc-cell">
      <div class="nc-icon">🤖</div>
      <p class="nc-title">AI as the co-founder</p>
      <p class="nc-desc">Claude and GPT-4 handle the regulatory parsing, summary generation, and alert logic. The AI does the heavy lifting so users don't have to.</p>
    </div>
    <div class="nc-cell">
      <div class="nc-icon">🚀</div>
      <p class="nc-title">Shipped in 90 days</p>
      <p class="nc-desc">From idea to waitlist to launch in under 3 months. Speed is the advantage when you remove engineering bottlenecks from the equation.</p>
    </div>
  </div>
</section>

<hr class="sep"/>

<!-- TESTIMONIALS -->
<section class="section" style="padding-top:0;">
  <div class="section-head reveal">
    <p class="section-eyebrow">What early users say</p>
    <h2 class="section-title">Trusted by founders<br/>who hate compliance.</h2>
  </div>
  <div class="testimonial-grid reveal">
    <div class="tcard">
      <p class="tcard-quote">"Regflow saves me 4 hours a week I used to spend manually scanning regulatory sites. The plain-English summaries are genuinely useful."</p>
      <div class="tcard-author">
        <div class="tcard-avatar">SR</div>
        <div>
          <p class="tcard-name">Sarah R.</p>
          <p class="tcard-role">Fintech Founder, London</p>
        </div>
      </div>
    </div>
    <div class="tcard">
      <p class="tcard-quote">"As a non-lawyer building in a regulated space, this is the tool I didn't know I needed. Alerts me before things become problems."</p>
      <div class="tcard-author">
        <div class="tcard-avatar">JM</div>
        <div>
          <p class="tcard-name">James M.</p>
          <p class="tcard-role">HealthTech CEO, Berlin</p>
        </div>
      </div>
    </div>
    <div class="tcard">
      <p class="tcard-quote">"The checklist feature is brilliant. One click and I know exactly what my team needs to do to stay compliant. Nothing else does this."</p>
      <div class="tcard-author">
        <div class="tcard-avatar">PK</div>
        <div>
          <p class="tcard-name">Priya K.</p>
          <p class="tcard-role">SaaS Operator, Singapore</p>
        </div>
      </div>
    </div>
  </div>
</section>

<hr class="sep"/>

<!-- SAAS TOOLS -->
<section id="tools" class="section tools-section">
  <div class="tools-header section-head reveal">
    <p class="section-eyebrow">SaaS Tools</p>
    <h2 class="section-title">Built for the<br/>regulated world.</h2>
    <p class="section-desc">Two tools from the Regflow ecosystem — one powerful web app, one completely free Google Workspace solution.</p>
  </div>

  <!-- Two main tool cards -->
  <div class="tools-duo reveal">

    <!-- Tool 1: Regresolve AI Web App -->
    <div class="tool-card">
      <div class="tool-card-accent accent-blue"></div>
      <div class="tool-top">
        <div class="tool-icon-wrap">⚖️</div>
        <span class="tool-badge-paid">Web App · Paid</span>
      </div>
      <p class="tool-name">Regresolve AI</p>
      <p class="tool-tagline">Regulatory resolution, on demand</p>
      <p class="tool-desc">Regresolve AI is a full web application that helps compliance teams and founders instantly resolve regulatory queries. Paste in a regulation, ask a question, and get a structured, actionable resolution — no legal background required. Built for speed, designed for non-lawyers.</p>
      <ul class="tool-features">
        <li><span class="fi fi-blue"></span>AI-powered regulatory query resolution in seconds</li>
        <li><span class="fi fi-blue"></span>Supports GDPR, FDA, FCA, SEC and 40+ frameworks</li>
        <li><span class="fi fi-blue"></span>Plain-English answers with cited regulation references</li>
        <li><span class="fi fi-blue"></span>Saved resolution history and team workspace</li>
        <li><span class="fi fi-blue"></span>Export resolutions as PDF or shareable link</li>
        <li><span class="fi fi-blue"></span>No-code built — Bubble + OpenAI + Stripe</li>
      </ul>
      <div class="tool-stack-row">
        <span class="stack-chip">Bubble</span>
        <span class="stack-chip">OpenAI</span>
        <span class="stack-chip">Stripe</span>
        <span class="stack-chip">Zapier</span>
        <span class="stack-chip">Airtable</span>
      </div>
      <div class="tool-actions">
        <a href="#contact" class="tool-btn-primary">
          Try Regresolve
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none"><path d="M2.5 6h7M6.5 3.5L9 6l-2.5 2.5" stroke="currentColor" stroke-width="1.25" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
        <a href="#contact" class="tool-btn-outline">Book a demo</a>
      </div>
    </div>

    <!-- Tool 2: Free Regresolve Google Stack -->
    <div class="tool-card">
      <div class="tool-card-accent accent-green"></div>
      <div class="tool-top">
        <div class="tool-icon-wrap">📊</div>
        <span class="tool-badge-free">Google Stack · Free</span>
      </div>
      <p class="tool-name">Regresolve<br/>Google Stack</p>
      <p class="tool-tagline">Free forever · Google Workspace</p>
      <p class="tool-desc">A completely free, ready-to-use regulatory intelligence toolkit built entirely inside Google Sheets, Docs, and Forms. No account needed, no subscription. Just copy the template, connect your OpenAI key, and you have a working compliance resolver inside tools you already use every day.</p>
      <ul class="tool-features">
        <li><span class="fi"></span>Google Sheets as your regulation database</li>
        <li><span class="fi"></span>Google Forms for submitting compliance queries</li>
        <li><span class="fi"></span>Google Docs auto-generated resolution reports</li>
        <li><span class="fi"></span>Apps Script + OpenAI API for AI resolution logic</li>
        <li><span class="fi"></span>One-click template copy — zero setup required</li>
        <li><span class="fi"></span>100% free, open, and self-hosted in your Drive</li>
      </ul>
      <div class="tool-stack-row">
        <span class="stack-chip">Google Sheets</span>
        <span class="stack-chip">Google Docs</span>
        <span class="stack-chip">Apps Script</span>
        <span class="stack-chip">OpenAI API</span>
      </div>
      <div class="tool-actions">
        <a href="#contact" class="tool-btn-primary">
          Get free template
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none"><path d="M2.5 6h7M6.5 3.5L9 6l-2.5 2.5" stroke="currentColor" stroke-width="1.25" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
        <a href="#contact" class="tool-btn-outline">See how it works</a>
      </div>
    </div>
  </div>

  <!-- Stats strip below both cards -->
  <div class="tool-divider-row reveal reveal-d1">
    <div class="tool-stat-cell">
      <div class="tsc-val">2</div>
      <div class="tsc-lbl">Live tools in the ecosystem</div>
    </div>
    <div class="tool-stat-cell">
      <div class="tsc-val">Free</div>
      <div class="tsc-lbl">Google Stack — always free</div>
    </div>
    <div class="tool-stat-cell">
      <div class="tsc-val">40+</div>
      <div class="tsc-lbl">Regulatory frameworks supported</div>
    </div>
  </div>
</section>

<hr class="sep"/>

<!-- CONTACT / CTA -->
<section id="contact">
  <div class="cta-glow"></div>
  <div class="cta-inner">
    <div class="cta-tag reveal">Limited early access open</div>
    <h2 class="cta-title reveal reveal-d1">Stay ahead of<br/>every regulation.</h2>
    <p class="cta-desc reveal reveal-d2">Join 500+ founders already using Regflow AI to monitor regulatory changes, stay compliant, and focus on what actually matters — building the product.</p>
    <div class="cta-actions reveal reveal-d3">
      <a href="mailto:hello@regflow.ai" class="btn-primary-lg">
        Get early access
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7h8M8 4.5L10.5 7 8 9.5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </a>
      <a href="#" class="btn-outline-lg">Book a demo</a>
    </div>
    <div class="social-row reveal" style="transition-delay:0.25s">
      <a href="#" class="social-btn">𝕏 Twitter</a>
      <a href="#" class="social-btn">LinkedIn</a>
      <a href="#" class="social-btn">Product Hunt</a>
      <a href="mailto:hello@regflow.ai" class="social-btn">Email</a>
    </div>
  </div>
</section>

<footer>
  <div class="footer-brand">
    <div class="nav-logo-mark" style="width:22px;height:22px;">
      <svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
        <rect x="2" y="2" width="5" height="5" rx="1" fill="#0a0a0a"/>
        <rect x="9" y="2" width="5" height="5" rx="1" fill="#0a0a0a"/>
        <rect x="2" y="9" width="5" height="5" rx="1" fill="#0a0a0a"/>
        <rect x="9" y="9" width="5" height="5" rx="1" fill="#0a0a0a" opacity="0.4"/>
      </svg>
    </div>
    <span class="footer-brand-name">Regflow AI</span>
    <span class="footer-copy" style="margin-left:1rem;">© 2025</span>
  </div>
  <div class="footer-links">
    <a href="#">Privacy</a>
    <a href="#">Terms</a>
    <a href="#">Status</a>
    <a href="mailto:hello@regflow.ai">Contact</a>
  </div>
</footer>

<script>
const obs = new IntersectionObserver(entries => {
  entries.forEach(e => { if(e.isIntersecting) e.target.classList.add('visible'); });
}, { threshold: 0.07 });
document.querySelectorAll('.reveal').forEach(el => obs.observe(el));
</script>
</body>
</html>